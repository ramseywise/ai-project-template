// Landing page -- public per src/middleware.ts's PUBLIC_PATHS. Replace with
// the real marketing/landing content; this only proves the auth loop works.
export default function Home() {
  return (
    <main>
      <h1>{{ project_name }}</h1>
      <p>{{ project_description }}</p>
      <a href="/login">Sign in</a>
    </main>
  );
}
